# HRMS 前端构建
FROM node:22-alpine AS builder

# 允许 pnpm 构建指定包的脚本
ENV PNPM_SCRIPT_SHELL=/bin/sh
ENV COREPACK_ENABLE_STRICT=0

WORKDIR /app

# 复制依赖文件（前端目录）
COPY continew-admin-ui/package.json continew-admin-ui/package-lock.json continew-admin-ui/pnpm-lock.yaml ./

# 安装依赖 - 用 npm 安装 pnpm，再用 pnpm approve-builds + install
RUN npm install -g pnpm@9.15.4 && \
    pnpm approve-builds @vue-office/docx @vue-office/excel @vue-office/pdf es5-ext esbuild vue-demi vue-echarts 2>/dev/null; \
    pnpm install --frozen-lockfile --prefer-offline 2>&1 | grep -v "WARN\|ERR_PNPM_IGNORED_BUILDS\|approve-builds"

# 复制源代码
COPY continew-admin-ui/ .

# 构建生产版本
RUN pnpm build --no-typecheck 2>&1 || pnpm build 2>&1

# ========== 运行阶段 ==========
FROM nginx:1.25-alpine

# 设置时区
RUN apk add --no-cache tzdata \
    && cp /usr/share/zoneinfo/Asia/Shanghai /etc/localtime \
    && echo "Asia/Shanghai" > /etc/timezone \
    && apk del tzdata

# 移除默认配置
RUN rm -f /etc/nginx/conf.d/default.conf

# 复制 nginx 配置
COPY deploy/nginx/nginx.conf /etc/nginx/conf.d/default.conf

# 复制构建产物
COPY --from=builder /app/dist /usr/share/nginx/html

EXPOSE 80

HEALTHCHECK --interval=30s --timeout=3s --retries=3 \
  CMD curl -sf http://localhost/ || exit 1

CMD ["nginx", "-g", "daemon off;"]
